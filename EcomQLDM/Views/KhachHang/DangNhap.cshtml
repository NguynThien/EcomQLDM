@model EcomQLDM.ViewModels.LoginVM
@{
    ViewData["Title"] = "Đăng Nhập";
}

<div class="container-fluid">
    <div class="row px-xl-5">
        <div class="col-lg-8">
            <h5 class="section-title position-relative text-uppercase mb-3"><span class="bg-secondary pr-3">Đăng Nhập</span></h5>
            <form asp-action="DangNhap" asp-route-returnurl="@ViewBag.ReturnUrl">
                <div class="text-danger" asp-validation-summary="All"></div>
                <div class="row">
                    <div class="col-md-8 form-group">
                        <label asp-for="@Model.UserName"></label><sup>*</sup>
                        <input class="form-control" asp-for="@Model.UserName" />
                        <span asp-validation-for="@Model.UserName" class="text-danger"></span>
                    </div>
                    @* <div class="col-md-8 form-group">
                        <label>Email</label><sup>*</sup>
                        <input class="form-control" asp-for="@Model.UserName" />
                        <span asp-validation-for="@Model.UserName" class="text-danger"></span>
                    </div> *@
                    <div class="col-md-8 form-group">
                        <label asp-for="@Model.Password"></label><sup>*</sup>
                        <input class="form-control" asp-for="@Model.Password" />
                        <span asp-validation-for="@Model.Password" class="text-danger"></span>
                    </div>

                </div>

                <div class="row">
                    <div class="col-md-8 form-group">
                        <button type="submit" class="btn btn-block btn-primary font-weight-bold py-2">Đăng Nhập</button>
                        @* <a asp-controller="KhachHang" asp-action="ForgotPassword" class="btn btn-block btn-primary font-weight-bold py-2">Quên Mật Khẩu</a> *@
                    </div>
                </div>

            </form>
        </div>
    </div>
</div>

